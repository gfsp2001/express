import { clickAndWrite, click } from "../helpers/utilClicks.js";
import { arrMovements } from "../common/metadata.js";

export default async (token) => {

    // await click(arrMovements[0].x, arrMovements[0].y, "left", true, 3000);
    // await clickAndWrite(arrMovements[1].x, arrMovements[1].y, "p_gsanti", 1000)
    // await clickAndWrite(arrMovements[2].x, arrMovements[2].y, "2587734Janeth!.", 1000)
    // await click(arrMovements[3].x, arrMovements[3].y, "left", false, 5000);
    // await clickAndWrite(arrMovements[4].x, arrMovements[4].y, token, 15000)
    console.log("TOKEN: " + token);
    
}